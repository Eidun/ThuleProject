<p id="notice"><%= notice %></p>

<h1>Pecados</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sins.each do |sin| %>
      <tr>
        <td><%= sin.name %></td>
        <td><%= link_to 'Mostrar', sin %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Otro pecado', new_sin_path %>
