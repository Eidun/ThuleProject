<%= form_with(model: sin, local: true) do |form| %>
  <% if sin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sin.errors.count, "error") %> prohibited this sin from being saved:</h2>

      <ul>
      <% sin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label 'Nombre' %>
    <%= form.text_field :name, id: :sin_name %>
  </div>

  <div class="field">
    <%= form.label 'Circunstancias' %>
    <%= form.text_field :reason, id: :sin_reason %>
  </div>

  <div class="actions">
    <%= form.submit "Otro"%>
  </div>
<% end %>
